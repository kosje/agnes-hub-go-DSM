基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

### 本次更新（1.0.16）

**修复「在 AI 客户端里生图，不出图、只显示一串莫名其妙的文字」。**

你截图里的现象（正文变成 `<craft_mode> You are now in Agent mode…`）是两个缺陷叠在一起：

**1. 提示词取错了 —— 网关把客户端自己的框架指令当成了用户的话**

AI 客户端会在对话尾部追加自己的框架块（`<craft_mode>`、`<system-reminder>`、`<user_info>`…），
这些块同样以 `role=user` 出现。网关原来的逻辑是「取最后一条 user 消息」，于是：

- 发给上游的提示词变成了 `<craft_mode>You are now in Agent mode…`，
  **你说的「水墨国风佳人」根本没送出去**（生成结果自然也不对）；
- 上游把收到的提示词原样回显在 `revised_prompt` 里，前端拿它当图片的 alt 文本，
  于是你看到的就是那串框架指令。

现在会跳过纯框架注入块，往前找用户真正说的那句话；`<user_query>…</user_query>`
这类「包着真实输入」的标签会自动拆开取内层。判断规则不靠枚举标签名
（各家客户端命名五花八门，枚举必然漏），而是「开头整段都是标签块 → 视为注入」。

> 刻意只剥**开头**的标签块，不做全文删除：你完全可能问「把 `<div>foo</div>` 居中」，
> 全文删标签会把代码片段一起吃掉。

**2. 图片地址是相对路径 —— 外部客户端没法解析**

之前返回的是 `/api/chat/images/xxx.png`。这在网关自己的网页里没问题（浏览器按页面
origin 解析），但 AI 客户端拿到的只是一段 Markdown，**没有任何基准可以解析这个相对
路径** —— 图片必然加载失败，客户端只能退化成显示 alt 文本。

现在返回**绝对地址**（如 `http://你的NAS:4142/api/chat/images/xxx.png`）：

- 默认按请求头自动推断（优先 `X-Forwarded-Host` / `X-Forwarded-Proto`，其次 `Host`）；
- 走反向代理、或网关监听地址与客户端访问地址不一致时，在
  **设置 → 对外访问地址** 里显式填写最稳。

**3. 顺带：媒体回放接口改为免鉴权**

外部客户端没有、也不可能带上网关的会话 cookie。原来要求登录，意味着
**客户端里永远是一张裂图**。文件名是内容的 sha256 前 16 字节（128 位），
既不可枚举也不可猜测 —— 这是一个 capability URL（与 S3 预签名链接同一类做法）。
这也与 `/api/chat/v1/*` 那批生成接口的信任模型一致：那些接口本来就免鉴权，
而且危害更大（能直接消耗你的账号配额），媒体回放只是读一份已经生成好的文件。

> 如果你的网关暴露在公网、且不希望图片能被链接持有者访问，请告诉我，
> 我可以加一个开关把媒体回放重新锁回登录态。

---

### 上一版（1.0.15）

**生成的视频也落盘保存了，和图片一样。** 之前播放器拿到的是上游 CDN 地址
（浏览器能不能访问到、地址会不会过期都不在网关控制内）；现在服务端把产出取回来存到
数据目录的 `videos/`，聊天窗口和「视频任务」页播放的都是**本地副本**。

与图片不同的几个关键点：

- **不读进内存**：视频 10~30 MB，改成边下边写临时文件、边算哈希、原子改名；
- **上限 200 MB / 超时 5 分钟**（图片是 6 MB / 30 秒）；
- **扩展名优先按 URL 后缀判定**（CDN 给视频常回 `application/octet-stream`，否则会存成 `.bin`）；
- **清理按「条数 + 总字节」双重淘汰**，默认总上限 2 GB（设置里可改，0 = 不限制）；
- **回放支持 Range**，进度条可拖动、可断点续传。

「视频任务」页新增「产出」列，点一下直接在控制台播放本地副本。

---

### 上一版（1.0.14）

1. **修复生视频一直报 400 —— 上游原话是 `mode is required`**。
   Agnes 的视频模型分两代，参数口径**完全不兼容**：

   | | Agnes Video 2.5（推荐） | Agnes Video V2.0（已下线） |
   |---|---|---|
   | `mode` | **必填**：`text` / `keyframe` / `reference` | 可选：`ti2vid` / `keyframes` |
   | 时长 | `seconds` 字符串 `"4"`~`"12"` | `num_frames` + `frame_rate` |
   | 分辨率 | `size` 档位 `720P`/`1080P`/`1K`/`2K` | `width` + `height` 像素 |
   | 明确禁止 | **`width`/`height`/`num_frames`/`frame_rate` 一律 400** | — |

   现在按家族分两套口径，`mode` 按你给的素材自动推断（只写提示词 → `text`，
   给首/尾帧 → `keyframe`，给参考图 → `reference`）。

2. **修复 JSON 数组型字段被静默丢弃**：判空用了一个只认标量的转换函数，
   遇到 JSON 数组返回空串，导致 `"images": ["https://..."]` 这类参考素材写法发不出去。
   生图链路同样受影响。

---

### 上一版（1.0.13）

1. **版本号规则改为纯 `x.y.z`**：发新版直接 +1，不再带 `-0001` 构建号。
2. **生成的图片改为落盘保存**，聊天窗口显示本地链接。
3. **修复生视频链路的两个缺陷**：网页提交不落任务记录、查询任务被当成「再提交一次」。
4. **上游错误信息不再只剩一个「HTTP 400」**（这次能查到 `mode is required` 就是靠它）。
5. **新增控制台「向上游拉取可用模型」**（账号池 → 账号列表右上角）。
6. **旧品牌标识全面清理**（升级后需重新登录一次控制台，口令本身不受影响）。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.16.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.16.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.16.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.16.spk)

---

### 升级后建议检查一项设置

到 **设置 → 对外访问地址** 看一眼：

- **留空**（默认）→ 网关按你访问它用的地址自动推断，绝大多数情况够用；
- **填上**（如 `http://192.168.1.10:4142`）→ 走反向代理、或你从不同地址访问网关时更稳。

如果客户端里还是不出图，可以先在浏览器里直接打开那个地址试试
（形如 `http://你的NAS:4142/api/chat/images/xxxx.png`）—— 能打开说明落盘和回放都正常，
问题就只剩客户端那边的渲染了。
