基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

### 本次修复（1.0.11-0002）

1. **重做套件图标，彻底解决「图标显示不对」**：
   - 旧图标是「深蓝方底 + 浅紫圆盘 + 白色字形」的三层叠加：上游 `assets/ICON.PNG` 本身是「黑底 + 浅紫圆盘 + 纯白字形」的 RGB 图（无 alpha），抠掉黑底后白字形直接压在浅紫圆盘上，对比度极低，圆盘还会与方底形成两层轮廓互相打架。
   - 现在改为**从源图量出字形的真实几何再按矢量精度重绘**：用白色阈值分离出字形，量出外框尺寸、边框厚度与两条分隔线的位置，然后在目标尺寸上重新栅格化（逐行求水平跨度、按像素算覆盖度做抗锯齿）。16/24/32/48/64/72/128/256 全尺寸都清晰、无锯齿、无糊边。
   - 视觉上改为**品牌蓝渐变圆角方底 + 白色字形 + 淡蓝三格**，保住原图「三格面板」的语义，在 DSM 浅色与深色主题下都清晰。
   - 顺手修正了原图字形**偏左 5.5px 未居中**的问题，现在水平垂直居中。
   - 几何是从源图量出来的，上游换 logo 也能自适应；量不出来直接报错，不会静默产出错误图标。

2. **群晖 DSM 7 主菜单与桌面快捷方式不显示（1.0.11-0001 已修）**：
   - 将 `ui/` 目录打入 `package.tgz` 内部，满足 DSM 安装服务自动建立 `/usr/syno/synoman/webman/3rdparty/agnes-hub` 软链接的规范；
   - 在 `postinst` 增加软链接自检与兜底，确保安装后主菜单立即出现快捷图标；
   - 精简 `ui/config`，移除可能引发 DSM 权限拦截的非标字段。

3. **同步上游 v1.0.11 核心能力**：
   - 控制台账号编辑改为完整弹窗（支持名称/Key/BaseURL/类型/分组/并发/优先级/兜底模型/模态清单/RPM覆盖）；
   - 新增账号调用优先级与按账号 RPM 覆盖，调度按优先级、区域与预计等待时间排序；
   - 修复软粘性会话绑定不校验请求模型、错发 AMD 渠道的缺陷；
   - 修复 AMD 经网关返回空体（上游响应体被提前关闭）的问题。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.11-0002.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.11-0002.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.11-0002.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.11-0002.spk)

---

### 升级后图标没变？

DSM 会把图标缓存在 `/usr/syno/synoman/webman/3rdparty/<套件名>` 与浏览器的图标缓存里。升级后如果桌面还是旧图标：

1. 在套件中心确认版本已是 `1.0.11-0002`；
2. **强制刷新浏览器**（Windows `Ctrl+F5` / macOS `Cmd+Shift+R`），或直接换个浏览器/无痕窗口验证；
3. 仍然不对就重启一次 DSM 桌面会话（右上角头像 → 重新载入），或执行 `sudo synoservice --restart nginx` 清掉 nginx 的静态资源缓存。
