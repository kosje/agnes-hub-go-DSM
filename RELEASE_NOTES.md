基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

> **版本号规则变了**：从这一版起只有一个版本号，发新版直接 +1（`1.0.13` → `1.0.14`），
> 不再带 `-0001` 这种构建号。套件 INFO、套件源 catalog、控制台里显示的「当前版本 / 最新版本」、
> GitHub Release 的 tag 全部来自同一个值，不会再出现「装的是 1.0.12-0002、界面显示 1.0.12」这种对不上的情况。

### 本次更新（1.0.13）

1. **生成的图片改为落盘保存，聊天窗口显示本地链接**（按你的要求改掉了 base64 方案）：

   - 上一版是把图片转成 base64 内联进正文。能显示，但代价很大：一张 3 MB 的图转 base64 要 4 MB，
     正文里放一份、`images[]` 里再放一份，一个响应能涨到 8 MB，聊天记录也会被单条记录撑爆。
   - 现在由服务端把图片取回来**存到数据目录**（`<数据目录>/images/`），正文里只放一个短短的本地地址
     `/api/chat/images/<内容指纹>.png`。响应小、记录小，而且**翻聊天记录时图片还能重新显示出来**。
   - 文件名是内容的 sha256，同一个文件天然去重；地址与内容一一对应，所以敢发长期缓存头。
   - 缓存目录有容量上限（沿用设置里的「图片最大条数」，默认 500），超出自动淘汰最旧的，
     不会把 NAS 慢慢填满。
   - 取不回来时退回上游地址，绝不因为一张图让整个响应失败。

2. **修复生视频链路**（你截图里的 HTTP 400）：

   - **错误信息不再是光秃秃一个「HTTP 400」**。上游各家 relay 的错误字段名并不统一
     （`message` / `msg` / `detail` / `error` 是字符串…），以前只认 `error.message`，
     认不出就只剩一个状态码，完全看不出是模型不可用、额度不够还是参数不合法。
     现在会把上游原话挖出来显示，实在挖不到也会附上原始响应开头。
   - **新增「向上游拉取可用模型」按钮**（控制台 → 账号 → 账号列表上方）。
     它拿每个账号自己的 Key 去问上游 `GET /v1/models`，把「上游实际给的」和
     「你本地清单里填的」对一遍，**直接标出哪些模型上游没给**。
     这正是判断「上游是不是把三个视频模型下架了」最直接的办法 —— 控制台里的模型清单是手填的，
     上游改了可售清单本地不会知道。
   - **修掉两个真实缺陷**：网页提交的视频以前不落任务记录，前端也拿不到 `job_id`
     （上游只回 `video_id`），提交完就没有下文；而查询任务的路由会被当成「再提交一次」，
     以 `POST /v1/videos/<id>` 发给上游，上游只会回 400 —— 视频即使提交成功也永远等不到结果。
     现在提交会返回本地 `job_id` 并落进「视频任务」，查询走上游真正的查询接口（`GET /agnesapi`）。
   - 前端轮询加了上限（约 5 分钟）和「任务不存在」的明确提示，不再无限空转。

3. **控制台「版本与自更新」面板**：

   - 套件版**仍然能查到最新版本号与发布时间**（之前把「能不能查」和「能不能装」合成了一个开关，
     导致套件版这两格永远显示「—」）；
   - 「发布时间」精确到分钟；
   - 「查看全部版本」改成一个绿色的 **GitHub 徽标链接**（鼠标悬停显示「在 GitHub 查看项目仓库」），
     地址跟着后端实际使用的仓库走。

4. **旧品牌标识全面清理**（你说可以重置，所以全改了）：

   | 位置 | 改成 |
   |---|---|
   | Prometheus 指标名 `baiPiao_hub_*` | `agnes_hub_*` |
   | 响应字段 `baipiao_hub` | `agnes_hub` |
   | 会话派生串 / cookie 名 | `agnes_hub_session`、`agnes_chat_password` |
   | 会话请求头 `x-baipiao-session` | `x-agnes-session` |
   | 发往上游的 User-Agent | `agnes-hub/1.0` |
   | 进程标志、启动脚本 | `--agnes-hub-swap-helper`、`agnes-hub-go.bat` |

   > **副作用**：会话 cookie 名变了，升级后**需要重新登录一次**控制台与对话页。
   > 口令本身不受影响（口令哈希用的是每个实例自己的随机 salt，与品牌串无关）。
   > 如果你接了这个套件的 Prometheus 指标，看板里的指标名要同步改。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.13.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.13.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.13.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.13.spk)

---

### 升级后图标 / 页面还是旧的？

1. 在套件中心确认版本已是 `1.0.13`；
2. **强制刷新浏览器**（Windows `Ctrl+F5` / macOS `Cmd+Shift+R`），或换浏览器 / 无痕窗口验证；
3. 顶栏 logo 的缓存问题已从根上修掉（指纹化 URL），理论上刷新即可；若仍不对，再执行
   `sudo synoservice --restart nginx` 清掉 nginx 的静态资源缓存。

---

### 升级到 1.0.13 之后建议先做一件事

如果你之前生视频一直失败，请到 **控制台 → 账号 → 「向上游拉取可用模型」** 点一下：

- 如果 **video 模型一个都没有** → 说明你的上游账号确实没有开视频权限（或已下架），
  这是上游侧的限制，不是网关的问题；
- 如果 **只有你填在清单里的那几个缺失** → 按探测结果把账号的模型清单改成上游实际给的即可。
