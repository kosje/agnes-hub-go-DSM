基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

### 本次更新（1.0.15）

**生成的视频现在也落盘保存了，和图片一样。**

之前视频完成后，播放器拿到的是上游 CDN 的地址（`platform-outputs.agnes-ai.space/...`）——
浏览器能不能访问到、地址会不会过期，都不在网关控制之内。现在改成：视频一完成，
服务端就把产出取回来存到数据目录的 `videos/`，聊天窗口和「视频任务」页播放的都是**本地副本**。

几个和图片不一样、必须区别对待的地方：

1. **不能读进内存**。视频一段 10~30 MB，用图片那套「先读进内存再写盘」在内存吃紧的
   NAS 上很容易把进程顶爆。改成**边下边写临时文件**，边算内容哈希，下完再原子改名。
   超过上限就中断并删掉半成品。

2. **上限和超时不一样**。图片是 6 MB / 30 秒，视频放到 **200 MB / 5 分钟**。

3. **扩展名判定顺序不一样**。CDN 给视频的 `Content-Type` 经常是
   `application/octet-stream`，而 Go 的内容嗅探对视频也不如对图片可靠。
   所以视频**优先按 URL 后缀**定扩展名（`.mp4` / `.webm` / `.mov` / `.m4v` / `.mkv`），
   不然会存成 `.bin`，浏览器就当下载处理了。

4. **清理策略多了一道闸门**。只卡条数不够 —— 200 条 1080P 视频能吃掉好几个 GB。
   现在同时卡「条数」和「总字节」，**默认总上限 2 GB**，超出按最旧的先删。
   可以在 **设置 → 本地视频缓存上限（MB）** 里改，填 0 表示不限制。

5. **回放走 `http.ServeContent`**，自带 Range 支持 —— 视频才能拖动进度条、断点续传。

另外，「视频任务」页新增了一列「产出」，完成的记录点一下就能直接在控制台里播放本地副本。

> 顺带把图片和视频的落盘逻辑合并成了一套通用实现（`internal/web/mediastore.go`）。
> 之前图片那套是写死的，视频照抄会踩上面五个坑；现在两者的差异全部收在一处声明里，
> 改一边不会误伤另一边。

---

### 上一版（1.0.14）

1. **修复生视频一直报 400 —— 上游原话是 `mode is required`**。

   Agnes 的视频模型分成两代，参数口径**完全不兼容**：

   | | Agnes Video 2.5（当前推荐） | Agnes Video V2.0（上游已下线） |
   |---|---|---|
   | `mode` | **必填**：`text` / `keyframe` / `reference` | 可选：`ti2vid` / `keyframes` |
   | 时长 | `seconds` 字符串 `"4"`~`"12"` | `num_frames` + `frame_rate` |
   | 分辨率 | `size` 档位 `720P`/`1080P`/`1K`/`2K` | `width` + `height` 像素 |
   | 画幅 | `aspect_ratio` | — |
   | 关键帧 | `first_frame` / `last_frame` | `extra_body.image` |
   | 参考素材 | `images` / `audios` / `videos` | `image` |
   | 明确禁止 | **`width`/`height`/`num_frames`/`frame_rate` 一律 400** | — |

   而网关只有一份「v2.0 味」的白名单，里面根本没有 `mode`。所以选 `agnes-auto`
   （自动挑中 `agnes-video-2.5-flash`）必然缺 `mode`；就算补上，白名单里的
   `width` / `num_frames` 一并发过去还会再吃一个 400。

   现在按家族分两套口径，`mode` 会按你提供的素材自动推断（只写提示词 → `text`，
   给首/尾帧 → `keyframe`，给参考图 → `reference`），你显式指定且素材能满足就听你的；
   与 `mode` 冲突的媒体字段会自动清掉；`duration` 会自动映射成 `seconds`。

2. **顺带修掉一个更隐蔽的缺陷**：JSON 数组型字段被静默丢弃。

   代码里判断「字段是不是空的」用的是只认标量的转换函数，遇到 JSON 数组返回空串 ——
   而请求体解析成 map 之后，`"images": ["https://..."]` 正好就是数组。
   结果是**参考图、多图输入这类最正常的写法会被当成空值悄悄丢掉**。生图链路同样受影响。

3. 控制台账号页的「账号测试」按钮同步改用统一口径。

---

### 上一版（1.0.13）

1. **版本号规则改为纯 `x.y.z`**：发新版直接 +1，不再带 `-0001` 构建号。
   套件 INFO、套件源 catalog、控制台显示的「当前版本 / 最新版本」、Release tag 全部一致。
2. **生成的图片改为落盘保存**，聊天窗口显示本地链接；翻聊天记录时图片仍能显示。
3. **修复生视频链路的两个缺陷**：网页提交不落任务记录、查询任务被当成「再提交一次」。
4. **上游错误信息不再只剩一个「HTTP 400」**（这次能查到 `mode is required` 就是靠它）。
5. **新增控制台「向上游拉取可用模型」**（账号池 → 账号列表右上角）。
6. **旧品牌标识全面清理**（升级后需重新登录一次控制台，口令本身不受影响）。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.15.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.15.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.15.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.15.spk)

---

### 磁盘占用说明

落盘的产出都在数据目录下：

- 图片：`<数据目录>/images/`，上限「图库最大容量」（默认 500 个）
- 视频：`<数据目录>/videos/`，上限「视频库最大容量」（默认 200 个）**和**「本地视频缓存上限」（默认 2048 MB）

想省空间就把「本地视频缓存上限」调小；想全部留着就填 0（不限制）。
清理只会在有新产出落盘时触发，不会在后台空转。

---

### 升级后图标 / 页面还是旧的？

1. 在套件中心确认版本已是 `1.0.15`；
2. **强制刷新浏览器**（Windows `Ctrl+F5` / macOS `Cmd+Shift+R`），或换浏览器 / 无痕窗口验证；
3. 顶栏 logo 的缓存问题已从根上修掉（指纹化 URL），理论上刷新即可；若仍不对，再执行
   `sudo synoservice --restart nginx` 清掉 nginx 的静态资源缓存。
