基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

### 本次更新（1.0.19）

**三件事：图标去掉白圈、客户端改用网关地址、正文里给出可下载的原图地址。**

#### 1. 图标：那道白圈没了

原始素材在「A」外面套了一道细白圆环（圆心 `(208, 209)`、半径 192px）。在 256px 上
看着像星球光环，但在 DSM 桌面的 64px 图标上会读成一个「加载中」的转圈 ——
所以去掉了。

不能靠裁切解决：A 的外接框约 235×198，而圆环内圈半径只有 176。想把它完整框进
「圆环以内」需要边长 ≥ 250 的正方形，那样四个角又会探出圆环 —— 两者不可兼得，
只能改像素：沿半径方向插值把环带填掉，再在环带内做一次轻模糊抹掉放射状拖影。
**采样点必须紧贴环带**，取远了会补出一道比原图更难看的**黑圈**（这个坑踩过一次）。
做法与参数留在 `tools/remove_icon_ring.py`，换素材后重跑即可。

顺带把小尺寸的取景补全：原先只对 32/48 两档「往里收」，而 DSM 桌面取的正是
**64/72** —— 唯一没享受这档补偿的尺寸。现在 72 也收一点，「A」在桌面图标上更清楚。

#### 2. 客户端现在拿到的是网关地址

你填了「对外访问地址」、NPM 也配好了，客户端拿到的却还是上游 CDN 地址。
根因是设置页那个下拉框：它只有「上游地址 / 本机地址」两项、默认选中上游，
而**每次保存设置页都会把当前选中项写回配置** —— 只要你动过一次设置页
（哪怕只是想改图片保留天数），这个字段就被钉死成 `upstream`，你填的对外地址等于白填。

现在改成三档，默认是 **自动**：

| 档位 | 行为 |
|---|---|
| **自动**（默认） | 填了「对外访问地址」就用网关地址；没填才退回上游 |
| 上游地址 | 一律上游产出地址（最稳，但离线看不了） |
| 网关地址 | 一律网关地址 |

**升级时会自动迁移一次**：把「`upstream` + 已配对外访问地址」这种组合交回自动判定。
你现在的配置正好就是这个组合，所以**升级后不用再手动改任何设置**。
（若确实想用上游地址，在设置页重新选一次即可，不会再被迁移覆盖。）

#### 3. 自检面板不再骗人

上一版那个「查看当前生效的媒体地址」面板有个 bug：面板自己就是网页请求，
而它拿这次请求去推算「客户端将收到」，被网页标记短路成了网关地址 ——
于是**不管你怎么配，面板都显示「客户端拿到的是网关地址」**。

现在面板按「外部客户端」的口径如实计算，并显示生效档位
（例如「自动（已配对外访问地址 → 用网关地址）」）。

#### 4. 正文里多了一条可下载的原图地址

图片照旧内联显示，下面另起一行给出纯文本地址：

```
原图下载：https://agens.jr.tn:52325/api/chat/images/<hash>.png?download=1
```

Markdown 的 `![](url)` 把地址藏进了语法里，而 AI 客户端里图片是渲染出来的、
右键常常拿不到原始地址 —— 想「另存为」时无处可复制。带 `?download=1` 的话
点开即存盘（响应头 `Content-Disposition: attachment`，文件名形如
`agnes-1a2b3c4d.png`）；不带参数则照旧内联渲染，两者共用同一个文件。

---

### 上一版（1.0.18）

**继续修「客户端生图不出图、只显示一串文字」。上一版没修到点子上。**

你给的第二张截图（图片还没落盘时能正常显示）是关键证据：它证明**客户端能加载图片**，
只是加载不了指向你 NAS 的那个地址。而 `https://platform-outputs.agnes-ai.space/...`
是公网 HTTPS，所以能显示。

也就是说，问题不在「相对路径 vs 绝对路径」，而在**地址指向哪里**：

| 调用方 | 它在哪里 | 能加载什么地址 |
|---|---|---|
| 网关自己的网页（`/chat`、控制台） | 和网关同源 | 本地地址（**这正是落盘的意义**） |
| 外部 AI 客户端（WorkBuddy 等） | 另一个网络位置，甚至跑在 https 页面里 | 公网可达的地址 |

而我上一版把两者一视同仁，一律回本地地址 —— 客户端自然加载不了。

**现在的做法：按调用方分流。**

- 网关自己的网页：用本地副本（离线可看、翻记录也能显示）；
- 外部 AI 客户端：**默认回上游地址**（公网 HTTPS，实测能显示）。

新增 **设置 → 客户端媒体地址**，两个选项：

- **上游地址（默认，最稳）** —— 客户端一定能显示；
- **本机地址（需能访问到网关）** —— 如果你已经把网关反代到公网 HTTPS，
  改成这个就能让客户端也看本地副本，离线可看。

另外新增 **「查看当前生效的媒体地址」** 自检按钮（设置页底部）：它会把
「按当前这次请求推算出的基地址」摊开给你看，专门用来排查反向代理配置。

---

### 反代部署的一个坑（你这次正好撞上）

你已经用 NPM 把网关反代到了 `https://agens.jr.tn:52325`。**但 NPM 默认把
`Host` 与 `X-Forwarded-Host` 都设成 `$host`，是不带端口的** —— 网关自动推断出来
会变成 `https://agens.jr.tn`（少了 `:52325`），照样加载不了。

所以请这样配：

1. **设置 → 对外访问地址** 填 `https://agens.jr.tn:52325`（**端口不能省**）；
2. **设置 → 客户端媒体地址** 保持默认的 **「自动」** 即可 ——
   1.0.19 起，只要配了对外访问地址，客户端就自动用网关地址；
3. 保存后点一下 **「查看当前生效的媒体地址」**，确认：
   - 「客户端将收到」那一行是 `https://agens.jr.tn:52325/api/chat/images/....png`
   - 把「示例图片地址」粘到浏览器里能打开

如果不想动 NPM 的配置，也可以用另一种办法：在 NPM 的
**Advanced → Custom Nginx Configuration** 里加一行

```nginx
proxy_set_header X-Forwarded-Host $http_host;
```

（`$http_host` 带端口，`$host` 不带。）这样网关就能自动推断正确，
「对外访问地址」可以留空 —— 局域网访问时用局域网地址、外网访问时用外网地址，
两种入口都对。

---

### 上一版（1.0.17）

**重排了控制台「视频任务」页。** 之前那张表又宽又挤、带横向滚动条 ——
根因是 `job_id` 是 64 字符的不换行长串，把整张表撑爆了，其余列全被挤到折行
（「产出」标题甚至被竖排成两行）。

- 长 ID 收成「前 8…后 4」，完整值悬停可见、**点一下复制**；
- `job_id` 与 `video_id` 合并成一列「任务」（7 列 → 6 列）；
- 「承载账号」直接显示账号名（原来只有 `acc_xxx` 这种 ID）；
- 表头一律不换行；
- 新增「补取」按钮：本地落盘功能上线**之前**完成的任务没有本地副本，
  点一下回上游取回落盘。

---

### 上一版（1.0.16）

**修复「提示词被取成客户端自己的框架块」。**

AI 客户端会在对话尾部追加自己的框架块（`<craft_mode>`、`<system-reminder>`、
`<user_info>`…），这些块**同样以 `role=user` 出现**。网关原来的逻辑是
「取最后一条 user 消息」，于是发给上游的提示词变成了
`<craft_mode>You are now in Agent mode…`，**你说的「水墨国风佳人」根本没送出去**；
上游把它回显在 `revised_prompt` 里，前端拿它当图片的 alt 文本，
所以你看到的就是那串框架指令。

现在会跳过纯框架注入块，往前找用户真正说的那句话；`<user_query>…</user_query>`
这类包装标签会自动拆开取内层。

另外：媒体回放接口改为免鉴权 —— 外部客户端没有、也不可能带上网关的会话 cookie，
原来要求登录意味着客户端里永远是一张裂图。文件名是内容的 sha256 前 16 字节
（128 位），不可枚举，属于 capability URL。

---

### 上一版（1.0.15）

**生成的视频也落盘保存了。** 服务端把产出取回来存到数据目录的 `videos/`，
聊天窗口和「视频任务」页播放的都是本地副本。与图片不同的关键点：
不读进内存（边下边写临时文件）、上限 200 MB / 超时 5 分钟、
扩展名优先按 URL 后缀判定、按「条数 + 总字节」双重淘汰（默认 2 GB）、
回放支持 Range（进度条可拖动）。

---

### 上一版（1.0.14）

1. **修复生视频一直报 400 —— 上游原话是 `mode is required`**。
   Agnes 的视频模型分两代，参数口径**完全不兼容**（2.5 系列 `mode` 必填、
   用 `seconds`/`size`/`aspect_ratio`，且**明确禁止** `width`/`height`/`num_frames`/`frame_rate`；
   v2.0 反过来）。现在按家族分两套口径，`mode` 按你给的素材自动推断。

2. **修复 JSON 数组型字段被静默丢弃**：判空用了一个只认标量的转换函数，
   遇到 JSON 数组返回空串，导致 `"images": ["https://..."]` 这类参考素材写法发不出去。

---

### 上一版（1.0.13）

1. **版本号规则改为纯 `x.y.z`**：发新版直接 +1，不再带 `-0001` 构建号。
2. **生成的图片改为落盘保存**，聊天窗口显示本地链接。
3. **修复生视频链路的两个缺陷**：网页提交不落任务记录、查询任务被当成「再提交一次」。
4. **上游错误信息不再只剩一个「HTTP 400」**（1.0.14 能查到 `mode is required` 就是靠它）。
5. **新增控制台「向上游拉取可用模型」**（账号池 → 账号列表右上角）。
6. **旧品牌标识全面清理**（升级后需重新登录一次控制台，口令本身不受影响）。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.18.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.18.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.18.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.18.spk)

---

### 升级后的配置建议（反代用户）

| 设置项 | 值 |
|---|---|
| 对外访问地址 | `https://你的域名:端口`（**端口不能省**） |
| 客户端媒体地址 | 本机地址 |

改完点「查看当前生效的媒体地址」确认「客户端将收到」那一行是对的。

如果不设「对外访问地址」，就保持「客户端媒体地址 = 上游地址」——
这样客户端也能正常显示，只是看的是上游的图（上游地址过期后旧记录可能失效）。
