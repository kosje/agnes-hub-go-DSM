基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

### 本次更新（1.0.17）

**重排了控制台「视频任务」页 —— 之前那张表又宽又挤，还带一条横向滚动条。**

原因很直接：`job_id` 是 64 个字符的不换行长串，原样铺进表格就把整张表撑爆了，
其余列全被挤到折行 —— 「产出」这个两字标题甚至被竖排成了两行，模型名
`agnes-video-v2.0` 也被拆成三行。

改了什么：

1. **长 ID 收成「前 8…后 4」**，完整值放在鼠标悬停提示里，**点一下即可复制**。
   （复制带了兼容处理：控制台常通过局域网 http 访问，那种非安全上下文里
   浏览器的剪贴板 API 不可用，会自动退回旧办法。）

2. **`job_id` 与 `video_id` 合并成一列「任务」** —— 7 列变 6 列，宽度省下一大截。
   job_id 在上面，上游的 video_id 用小字灰色排在下面。

3. **「承载账号」直接显示账号名**。原来只有一串 `acc_1593c781bbf6` 这种 ID，
   根本看不出是谁；现在主显示账号名，ID 缩小排在下面、悬停可看全。

4. **表头一律不换行**（这条对整个控制台生效，其它表格也一起受益），
   模型名、状态、时间列也不折行。

5. **新增「补取」按钮**。你截图里那条 9/25 20:54 的任务状态是 `completed`
   但「产出」是空的 —— 因为它是本地落盘功能上线**之前**完成的，记录里没有地址。
   现在点「补取」就会回上游把产出取回本地，之后就能直接播放了。

6. **顺带修掉一个我上一版引入的 bug**：「播放」按钮判断的是「地址以
   `/api/chat/videos/` 开头」，而 1.0.16 起地址已改成绝对地址
   （`http://host/api/chat/videos/...`），于是明明有本地副本也会误报
   「该记录没有本地副本」。

---

### 上一版（1.0.16）

**修复「在 AI 客户端里生图，不出图、只显示一串莫名其妙的文字」。**
你截图里的现象（正文变成 `<craft_mode> You are now in Agent mode…`）是两个缺陷叠在一起：

**1. 提示词取错了 —— 网关把客户端自己的框架指令当成了用户的话**

AI 客户端会在对话尾部追加自己的框架块（`<craft_mode>`、`<system-reminder>`、
`<user_info>`…），这些块**同样以 `role=user` 出现**。网关原来的逻辑是
「取最后一条 user 消息」，于是发给上游的提示词变成了 `<craft_mode>You are now in
Agent mode…`，**你说的「水墨国风佳人」根本没送出去**；上游把它回显在
`revised_prompt` 里，前端拿它当图片的 alt 文本，所以你看到的就是那串框架指令。

现在会跳过纯框架注入块，往前找用户真正说的那句话；`<user_query>…</user_query>`
这类包装标签会自动拆开取内层。判断规则不靠枚举标签名（各家客户端命名五花八门，
枚举必然漏），而是「开头整段都是标签块 → 视为注入」。
只剥**开头**的标签块，不做全文删除 —— 你完全可能问「把 `<div>foo</div>` 居中」。

**2. 图片地址是相对路径 —— 外部客户端没法解析**

之前返回 `/api/chat/images/xxx.png`。这在网关自己的网页里没问题（浏览器按页面
origin 解析），但 AI 客户端拿到的只是一段 Markdown，**没有任何基准可以解析这个相对
路径** —— 图片必然加载失败，客户端只能退化成显示 alt 文本。

现在返回**绝对地址**：默认按请求头自动推断（优先 `X-Forwarded-Host` /
`X-Forwarded-Proto`，其次 `Host`）；走反向代理、或网关监听地址与客户端访问地址
不一致时，在 **设置 → 对外访问地址** 里显式填写最稳。

**3. 媒体回放接口改为免鉴权**：外部客户端没有、也不可能带上网关的会话 cookie，
原来要求登录意味着客户端里永远是一张裂图。文件名是内容的 sha256 前 16 字节
（128 位），不可枚举 —— 属于 capability URL。

---

### 上一版（1.0.15）

**生成的视频也落盘保存了。** 服务端把产出取回来存到数据目录的 `videos/`，
聊天窗口和「视频任务」页播放的都是本地副本。与图片不同的关键点：
不读进内存（边下边写临时文件）、上限 200 MB / 超时 5 分钟、
扩展名优先按 URL 后缀判定、按「条数 + 总字节」双重淘汰（默认 2 GB）、
回放支持 Range（进度条可拖动）。

---

### 上一版（1.0.14）

1. **修复生视频一直报 400 —— 上游原话是 `mode is required`**。
   Agnes 的视频模型分两代，参数口径**完全不兼容**（2.5 系列 `mode` 必填、
   用 `seconds`/`size`/`aspect_ratio`，且**明确禁止** `width`/`height`/`num_frames`/`frame_rate`；
   v2.0 反过来）。现在按家族分两套口径，`mode` 按你给的素材自动推断。

2. **修复 JSON 数组型字段被静默丢弃**：判空用了一个只认标量的转换函数，
   遇到 JSON 数组返回空串，导致 `"images": ["https://..."]` 这类参考素材写法发不出去。

---

### 上一版（1.0.13）

1. **版本号规则改为纯 `x.y.z`**：发新版直接 +1，不再带 `-0001` 构建号。
2. **生成的图片改为落盘保存**，聊天窗口显示本地链接。
3. **修复生视频链路的两个缺陷**：网页提交不落任务记录、查询任务被当成「再提交一次」。
4. **上游错误信息不再只剩一个「HTTP 400」**（1.0.14 能查到 `mode is required` 就是靠它）。
5. **新增控制台「向上游拉取可用模型」**（账号池 → 账号列表右上角）。
6. **旧品牌标识全面清理**（升级后需重新登录一次控制台，口令本身不受影响）。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.17.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.17.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.17.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.17.spk)

---

### 升级后建议检查一项设置

到 **设置 → 对外访问地址** 看一眼：

- **留空**（默认）→ 网关按你访问它用的地址自动推断，绝大多数情况够用；
- **填上**（如 `http://192.168.1.10:4142`）→ 走反向代理、或你从不同地址访问网关时更稳。

如果客户端里还是不出图，可以先把那个地址粘到浏览器里试试
（形如 `http://你的NAS:4142/api/chat/images/xxxx.png`）—— 能打开说明落盘和回放都正常，
问题就只剩客户端那边的渲染了。
