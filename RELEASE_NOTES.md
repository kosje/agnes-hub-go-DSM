基于 [my788525/agnes-hub-go](https://github.com/my788525/agnes-hub-go) **v1.0.11** 构建的群晖 DSM 套件。

### 本次更新（1.0.11-0003）

1. **换用全新的「A」字形图标**：
   - 图标源换成「铬质 A 字 + 上方圆点 + 星空光环」的插画，**整幅裁切**成方形图标；
   - 缩放改为在**线性光空间**做面积平均 —— 直接在 sRGB 上缩小会把暗部抬亮、亮部压暗，照片类图标缩完就是一张发灰的糊图；
   - 收尾做轻量提对比、提饱和与暗角，让图标读起来是一块完整的「瓦片」，而不是一张裁出来的图；
   - 套 DSM 圆角方形遮罩（0.22），并补齐 16/24/32/48/64/72/128/256 全套尺寸；
   - **小尺寸自适应**：16/24/32px 自动往里收一点，保证「A」在小图标里还认得出来；
   - 网页顶栏 logo 同步换成新图标 —— 原先还是「黑底 + 浅紫圆盘」的老图，22px 下白字形糊成一团。

2. **修复控制台「查看全部版本」指向错误仓库**：
   - 该链接原先写死成上游仓库（`my788525/baipiao-hub`），但上游 Release 里根本没有 SPK，用户点进去找不到套件；
   - 现在改为指向本套件仓库，且**链接跟随实际升级来源**：套件版指向本套件仓库，非套件版自更新开着时指向自更新仓库，保证链接与「立即更新」能装上的版本对得上。

3. **控制台如实显示套件版的升级方式**：
   - 套件是用 `-no-selfupdate` 启动的（进程内替换二进制会让套件的实际内容与 INFO 里登记的 checksum 对不上，下次升级必然冲突）；
   - 原先面板只显示「当前已是最新版本，无需更新」，会让用户以为没得升。现在显示「由套件中心管理」并置灰两个自更新按钮，明确提示到「套件中心 → 社群」升级。

4. **同步上游 v1.0.11 核心能力**（1.0.11-0001 起）：
   - 控制台账号编辑改为完整弹窗（支持名称/Key/BaseURL/类型/分组/并发/优先级/兜底模型/模态清单/RPM覆盖）；
   - 新增账号调用优先级与按账号 RPM 覆盖，调度按优先级、区域与预计等待时间排序；
   - 修复软粘性会话绑定不校验请求模型、错发 AMD 渠道的缺陷；
   - 修复 AMD 经网关返回空体（上游响应体被提前关闭）的问题。

5. **群晖 DSM 7 主菜单与桌面快捷方式不显示**（1.0.11-0001 起已修）：
   - 将 `ui/` 目录打入 `package.tgz` 内部，满足 DSM 安装服务自动建立 `/usr/syno/synoman/webman/3rdparty/agnes-hub` 软链接的规范；
   - 在 `postinst` 增加软链接自检与兜底；
   - 精简 `ui/config`，移除可能引发 DSM 权限拦截的非标字段。

---

### 推荐：添加群晖套件源（自动检查更新）

1. **套件中心** → **设置** → **常规** → **信任层级** 选择「**任何发行者**」；
2. **套件来源** → **新增**：
   - **x86_64 平台**（Intel / AMD 机型）：`https://kosje.github.io/agnes-hub-go-DSM/catalog.json`
   - **armv8 平台**（ARM64 机型，如 DS223/DS423 系列）：`https://kosje.github.io/agnes-hub-go-DSM/catalog-armv8.json`
3. 切换到套件中心「**社群**」标签页即可直接安装或一键更新。

---

### 手动安装下载

- **x86_64 机型**：[agnes-hub-x86_64-1.0.11-0003.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-x86_64-1.0.11-0003.spk)
- **armv8 机型**：[agnes-hub-armv8-1.0.11-0003.spk](https://kosje.github.io/agnes-hub-go-DSM/agnes-hub-armv8-1.0.11-0003.spk)

---

### 升级后图标没变？

DSM 与浏览器都会缓存图标。升级后如果桌面还是旧图标：

1. 在套件中心确认版本已是 `1.0.11-0003`；
2. **强制刷新浏览器**（Windows `Ctrl+F5` / macOS `Cmd+Shift+R`），或换浏览器 / 无痕窗口验证；
3. 仍然不对就重启一次 DSM 桌面会话（右上角头像 → 重新载入），或执行 `sudo synoservice --restart nginx` 清掉 nginx 的静态资源缓存。
