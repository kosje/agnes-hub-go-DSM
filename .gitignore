# 编译产物
*.exe~        # Go 链接器覆盖运行中的 exe 时留下的备份
*.exe
agnes-hub-go
agnes-hub-go-linux-amd64
agnes-hub-go-linux-arm64

# 运行期数据：含上游 API Key 与管理员口令散列，绝不提交
data/

# 验证脚本的临时产物
.smoke/
.smoke.log
.probe/
__pycache__/

# spk 构建产物（由 tools/build_spk.py 生成）
spk-bundle/
dist/
*.spk
# 例外：docs/ 下的 SPK 是套件源实际分发的文件，必须提交
!docs/*.spk
